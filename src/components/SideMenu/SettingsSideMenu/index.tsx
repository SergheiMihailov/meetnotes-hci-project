import React, { FunctionComponent } from "react";

const style = {
  background: "lightgreen",
  flex: 1,
};

const SideMenu: FunctionComponent = () => <div style={style}>SideMenu</div>;

export default SideMenu;
